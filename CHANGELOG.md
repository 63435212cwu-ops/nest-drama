# Changelog

所有显著变更按版本记录。版本号单一来源：`ui/serve.py` 的 `VERSION`（发布包名、`/api/health`、`X-NEST-Version` 响应头共用）。

## v1.1.0 — 2026-09-03

### 引擎（serve.py / dupian.py）
- **数目账（零 token）**：正文里说出口的数量自动入账并注入下一轮所有角色；悄悄换数判毙重推，明着质疑/改口放行并记为改口。
- **执法力度归一**：机检"密集毙"按篇幅归一（≥10 处，或 ≥5 处且 ≥4/千字）。
- **病谱 v4**：欲言又止、拟人沉默、含糊指代、一字一顿、时间虚化、回忆触发器；统计层新增破折号/省略号成瘾告警。
- **材料读取层**：.txt/.md/.csv/.json/.yaml/.srt（UTF-8/16/32、GB18030、Big5 自动嗅探）、.docx、.odt、.epub、.html、.rtf、.zip 递归展开、.pdf 尽力抽取；单文件 64 MB、单请求 512 MB 上限；解不出给可执行提示。
- **API 结构**：`GET /api/health`（版本、运行态、轮次、账本规模）、`GET /api/schema`（端点清单与约定）、`GET /api/formats`；所有 JSON 响应统一含 `ok`/`success`，失败含 `error`；`X-NEST-Version` 响应头。

### 界面（enhance.js / enhance.css，独立进化层）
- 日/夜双主题、玻璃顶栏、全局快捷键（`?` 查看）、卡司即时检索、成稿提醒、页签标题随剧情。
- 星丛电影级后期：ACES/AgX/Neutral、曝光、辉光、颗粒、暗角、色差、变形光斑、色调风格（含鎏金）；程序化星云与近景星尘；宽银幕影院模式与字幕条；PNG 导出；离屏暂停渲染。
- 跃迁动画（径向拖影 + 曝光闪 + FOV 推拉 + 标题卡）；"极简奢华"预设：静默名牌、连线减淡、彗尾缩短、呼吸镜头、金色发丝内框。
- 彗尾角跨度默认 144° → 72°，可调。

### 发布
- 打包器从 `serve.py` 读版本；隐私终检覆盖本机路径、用户名、密钥形态与本机私有词表。

## v1.0.0 — 2026-08-30
- 首个公开版本：世界构建、角色三卡、多角色隔离推演、监修官与毒编机检、星丛 3D 图谱、历史局与归档、发布打包器。
